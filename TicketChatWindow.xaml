<Window x:Class="MovieApp.TicketChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
        Title="Чат поддержки"
        Width="500" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="#141414"
        Foreground="White"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <!-- Header: Subject & Close Button (for Admins) -->
            <RowDefinition Height="Auto"/>
            <!-- Messages List -->
            <RowDefinition Height="*"/>
            <!-- Input area -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock x:Name="TxtTicketSubject" Text="Тема обращения" FontSize="18" FontWeight="Bold" Foreground="White" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="TxtTicketStatus" Text="Статус: Открыто" FontSize="12" Foreground="#888888" Margin="0,2,0,0"/>
            </StackPanel>
            <Button x:Name="BtnCloseTicket" Grid.Column="1" Content="Закрыть обращение" Background="#E50914" Foreground="White" BorderThickness="0" Padding="12,6" FontSize="12" FontWeight="SemiBold" Cursor="Hand" Click="BtnCloseTicket_Click" Visibility="Collapsed">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- Messages ListView -->
        <ListView x:Name="LvMessages" Grid.Row="1" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,15">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,4"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent"/>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                    </Style.Resources>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="{Binding Alignment}" MaxWidth="380" Margin="{Binding Margin}">
                        <Border CornerRadius="8" Padding="12,10" Background="{Binding BackgroundBrush}" BorderBrush="{Binding BorderBrush}" BorderThickness="1">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding SenderLogin}" FontWeight="Bold" FontSize="12" Foreground="LightGray" TextTrimming="CharacterEllipsis" Margin="0,0,10,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding TimeText}" FontSize="10" Foreground="#888888" VerticalAlignment="Center"/>
                                </Grid>
                                <TextBlock Text="{Binding Text}" Foreground="White" TextWrapping="Wrap" Margin="0,6,0,0" LineHeight="18" FontSize="13"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Chat Input Area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtMessageInput" Grid.Column="0" Height="40" Background="#242424" Foreground="White" BorderBrush="#444444" BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" FontSize="13" Tag="Введите сообщение..." GotFocus="TxtMessageInput_GotFocus" LostFocus="TxtMessageInput_LostFocus" KeyDown="TxtMessageInput_KeyDown"/>
            <Button x:Name="BtnSendMessage" Grid.Column="1" Content="Отправить" Width="100" Margin="10,0,0,0" Background="#0078D7" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand" Click="BtnSendMessage_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
        <metro:ProgressRing x:Name="ChatProgressRing" Grid.Row="1" Foreground="#E50914" Width="40" Height="40" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed"/>
    </Grid>
</Window>
