<Page x:Class="MovieApp.Pages.ManageContentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Управление контентом"
      Background="#141414">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая колонка: Добавление фильма -->
        <Border Grid.Column="0" Background="#1A1A1A" BorderBrush="#333333" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,10,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Добавить фильм" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

                    <TextBlock Text="Название:" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtAddTitle" Background="#2A2A2A" Foreground="White" BorderBrush="#444444" Padding="5" Margin="0,0,0,15"/>

                    <TextBlock Text="Год:" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtAddYear" Background="#2A2A2A" Foreground="White" BorderBrush="#444444" Padding="5" Margin="0,0,0,15"/>

                    <TextBlock Text="Рейтинг:" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtAddRating" Background="#2A2A2A" Foreground="White" BorderBrush="#444444" Padding="5" Margin="0,0,0,15"/>

                    <TextBlock Text="URL постера:" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtAddPoster" Background="#2A2A2A" Foreground="White" BorderBrush="#444444" Padding="5" Margin="0,0,0,15"/>

                    <TextBlock Text="Тип:" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <ComboBox x:Name="CmbAddType" Margin="0,0,0,15">
                        <ComboBoxItem Content="FILM" IsSelected="True"/>
                        <ComboBoxItem Content="TV_SERIES"/>
                    </ComboBox>

                    <TextBlock Text="Жанры:" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <ListBox x:Name="LstAddGenres" 
                             DisplayMemberPath="Name" 
                             SelectionMode="Multiple" 
                             Height="100" 
                             Background="#2A2A2A" 
                             Foreground="White" 
                             BorderBrush="#444444" 
                             Margin="0,0,0,25"/>

                    <Button Content="Добавить в базу" Background="#E50914" Foreground="White" BorderThickness="0" 
                            Padding="10" MinHeight="40" Margin="0,15,0,10" Cursor="Hand" Click="BtnAddMovie_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Правая колонка: Поиск и удаление -->
        <Border Grid.Column="1" Background="#1A1A1A" BorderBrush="#333333" BorderThickness="1" CornerRadius="8" Padding="20" Margin="10,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Удаление контента" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock Text="Поиск по названию..." Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtSearch" Background="#2A2A2A" Foreground="White" BorderBrush="#444444" Padding="8" TextChanged="TxtSearch_TextChanged"/>
                </StackPanel>

                <DataGrid x:Name="MoviesGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" Background="#141414" Foreground="White" BorderThickness="0" RowBackground="#1A1A1A" AlternatingRowBackground="#141414" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#333333" HeadersVisibility="Column">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#111111"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="10,5"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#333333"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="#1A1A1A"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#333333"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="10,5"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Год" Binding="{Binding Year}" Width="80"/>
                        <DataGridTemplateColumn Header="Действие" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Изменить" Width="Auto" Background="#0078D7" Foreground="White" BorderThickness="0" Padding="10,5" Margin="0,0,5,0" Cursor="Hand" Tag="{Binding Id}" Click="BtnEdit_Click"/>
                                        <Button Content="Удалить" Width="Auto" Background="#E50914" Foreground="White" BorderThickness="0" Padding="10,5" Cursor="Hand" Tag="{Binding Id}" Click="BtnDelete_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>
