<Page x:Class="MovieApp.Pages.UserSupportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Служба поддержки"
      Background="#141414">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Что привело вас сюда? *" Foreground="#AAAAAA" Margin="0,0,0,5"/>
        
        <ComboBox Grid.Row="1" x:Name="CmbReason" Margin="0,0,0,15" Background="#2A2A2A" Foreground="White">
            <ComboBoxItem Content="Проблема с аккаунтом" IsSelected="True"/>
            <ComboBoxItem Content="Техническая ошибка"/>
            <ComboBoxItem Content="Предложение по улучшению"/>
            <ComboBoxItem Content="Другое"/>
        </ComboBox>

        <TextBox Grid.Row="2" x:Name="TxtSupportMessage" MaxLength="1000" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Background="#2A2A2A" Foreground="White" Padding="10"/>

        <Button Grid.Row="3" Content="Отправить обращение" Background="#E50914" Foreground="White" BorderThickness="0" Padding="10" Margin="0,15,0,20" Cursor="Hand" Click="BtnSend_Click"/>

        <!-- История обращений -->
        <TextBlock Grid.Row="4" Text="История ваших обращений:" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>

        <ListBox x:Name="LstTickets" Grid.Row="5" Background="Transparent" BorderThickness="0" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="#1A1A1A" CornerRadius="8" Padding="15" Margin="0,0,15,15" 
                            BorderBrush="#333333" BorderThickness="1" Width="280" VerticalAlignment="Top">
                        <StackPanel>
                            <Grid>
                                <TextBlock Text="{Binding Reason}" FontWeight="Bold" Foreground="#E50914" FontSize="14" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Status}" HorizontalAlignment="Right" Foreground="#AAAAAA" FontSize="12"/>
                            </Grid>
                            <TextBlock Text="{Binding Message}" Foreground="White" TextWrapping="Wrap" Margin="0,8,0,0"/>
                            <TextBlock Text="{Binding CreatedAt, StringFormat=\{0:dd.MM.yyyy HH:mm\}}" Foreground="#666666" FontSize="11" Margin="0,5,0,0"/>
                            
                            <!-- Блок ответа администратора -->
                            <Border CornerRadius="4" Padding="10" Margin="0,10,0,0">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#252525"/>
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AdminReply}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding AdminReply}" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock Text="Ответ администратора:" FontWeight="Bold" Foreground="#0078D7" FontSize="12"/>
                                    <TextBlock Text="{Binding AdminReply}" Foreground="#D0D0D0" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
