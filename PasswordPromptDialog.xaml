<Window x:Class="MovieApp.PasswordPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подтверждение пароля"
        Height="240" Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#1F1F1F">

    <StackPanel Margin="28">
        <TextBlock Text="Введите пароль для отключения детского режима:"
                   Foreground="#B3B3B3" FontSize="13"
                   TextWrapping="Wrap"
                   Margin="0,0,0,14"/>

        <PasswordBox x:Name="PwdBox"
                     Background="#2A2A2A"
                     Foreground="White"
                     BorderBrush="#444444"
                     BorderThickness="1"
                     Padding="8"
                     FontSize="14"
                     Margin="0,0,0,18"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена"
                    Width="100" Height="34"
                    Background="#333333" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    Click="BtnCancel_Click"
                    Margin="0,0,10,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="8,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="OK"
                    Width="100" Height="34"
                    Background="#E50914" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    Click="BtnOk_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="8,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </StackPanel>
</Window>
